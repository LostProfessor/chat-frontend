# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

# ── 测试产物与凭据 ──
# testfiles/ 下是用于回归测试的二进制素材（含 2MB zip / 6.5MB png），不入库
testfiles/
# oldtoken.txt 里是真实 JWT，绝不能提交
oldtoken.txt

# ── 一次性调试脚本 ──
# 这三个是当时排查具体问题时写的临时工具（recall-* 验证撤回链路、
# ws-raw-probe 用裸 TCP 复现握手丢头 bug），只在本地保留以便复查，
# 不作为项目的一部分发布。
# ws-smoke-test.mjs 不在此列 —— 它是可复用的 6 项端到端集成测试，需要入库。
recall-step.mjs
recall-test.mjs
ws-raw-probe.mjs
